{
  "manifest_version": 2,

  "name": "Badge evaluator",
  "description": "Extension for showing the total price of a steam badge on the Steam Card Exchange.  Note that this makes the inventory page load EXTREMELY slow, so use with caution.",
  "version": "0.5",
  "content_scripts": [
    {
      "matches": [
        "http://*.steamcardexchange.net/*inventory*",
        "http://*.steamcardexchange.net/*userlist*"
      ],
      "js": ["jquery-2.1.4.min.js", "script.js"],
      "run_at": "document_start"
    }
  ]
}
